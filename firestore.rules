rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Temporarily allow all operations while fixing Firebase project setup
    // TODO: Re-enable authentication once Firebase project is properly configured
    match /{document=**} {
      allow read, write: if true;
    }
  }
}